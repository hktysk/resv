{
  "name": "multi-frame",
  "version": "1.0.0",
  "description": "Preview and auto-update websites under development with multiple device widths and heights",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "npx tsc --target es5 --lib es6,dom --esModuleInterop --downlevelIteration",
    "sass": "npx node-sass --no-source-map assets/css/ --output assets/css/",
    "pug": "npx pug assets/",
    "build": "npm run sass -- --watch & npm run pug -- --watch & npm run tsc assets/js/*.ts -- --watch & npx reload -b -d assets/ -w assets"
  },
  "author": "hktysk",
  "license": "MIT",
  "dependencies": {
    "fs": "0.0.1-security",
    "node-sass": "^4.12.0",
    "pug": "^2.0.4",
    "pug-cli": "^1.0.0-alpha6",
    "reload": "^3.0.2",
    "typescript": "^3.6.4"
  }
}
